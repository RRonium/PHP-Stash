<?php
	$str="Good Morning Sannidhya";
	//if(preg_match("/G.d/",$str))
	//if(preg_match("/^ning/",$str))
	//if(preg_match("/Good+/",$str))
	//if(preg_match("/Mor[anbc]ing/",$str))
	//if(preg_match("/Mor[a-m]ing/",$str))
	//if(preg_match("/Morning$/",$str))
	{
			echo "Pattern Matched";
	}
	else
	{
		echo "Pattern not Matched";
	}
?>